import styled from 'styled-components';

export const Hero = styled.section`
  padding: 2rem 0;
  max-width: 90%;
  width: 100%;
  margin: 0 auto;
  padding: 0 1.5rem;
`;

export const Hide = styled.div`
  overflow: hidden;
`